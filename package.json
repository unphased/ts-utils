{
  "name": "ts-utils",
  "version": "0.1.0",
  "description": "Slu's typescript utils",
  "exports": {
    ".": "./dist/utils.js",
    "./terminal": "./dist/terminal.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild terminal.ts --bundle --minify --sourcemap --platform=node --outfile=dist/terminal.js && esbuild utils.ts --bundle --minify --sourcemap --platform=node --format=esm --outfile=dist/utils.js"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/unphased/ts-utils.git"
  },
  "keywords": [
    "typescript",
    "library",
    "util",
    "helpers"
  ],
  "author": "Steven Lu",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/unphased/ts-utils/issues"
  },
  "homepage": "https://github.com/unphased/ts-utils#readme",
  "devDependencies": {
    "esbuild": "^0.20.1"
  }
}
